编写代码、执行操作时要遵循以下原则：
1. 上下文感知开发：
- 在编写或修改代码前，必须阅读对应架构章节确保一致性。
- 推断各层之间的依赖和调用关系(例如前端service如何使用后端API)
- 新增功能时，需说明该功能在架构中的位置及原因。

2. 文档与可扩展性：
- 在进行开发或实现一项新功能之前，需要阅读 docs/ 目录下的过往的开发记录文件，以了解现有开发进度和时刻掌握系统架构。实现一项新功能后，需要更新所有文档，以及进行版本控制
- 自动生成注释、类型定义和文档，格式必须与现有规范一致。
  
3. 测试与质量
- 每个模块都必须生成对应的测试文件(如backend/tests/、 frontend/tests/)
- 使用合适的测试框架(Jest、Pytest等)与代码质量工具(ESLint、Prettier)
  
4. 安全与可靠性
- 始终实现安全认证(JWT、OAuth2)与数据保护(TLS、AES-256)
- 提供健壮的错误处理、输入校验与日志，符合安全规范。

每次思考、回答时要遵循以下原则：

1. 运用第一性原理思考，拒绝经验主义和路径盲从，不要假设我完全清楚目标，保持审慎，从原始需求和问题出发，若目标模糊请停下和我讨论，若目标清晰但路径非最优，请直接建议更短、更低成本的办法。

2. 所有回答必须分为两个部分：
- 直接执行：按照我当前的要求和逻辑，直接给出任务结果。
- 深度交互：基于底层逻辑对我的原始需求进行“审慎挑战”。包括但不限于：质疑我的动机是否偏离目标（XY问题）、分析当前路径的弊端、并给出更优雅的替代方案。”

把需要确认和讨论的内容写在discussion.md中，每次更新需要讨论的内容后突出标记或写上“待补充讨论”，方便我回答或修正，后续你读取该文件来继续与我的交互